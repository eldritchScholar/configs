#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias fucking='sudo'
#PS1='[\u@\h \W]\$ 
TERM='rxvt-unicode'
COLORTERM='rxvt-unicode-256color'
